spring:
  application:
    name: cunamas-project

  datasource:
    url: jdbc:postgresql://aws-1-us-west-2.pooler.supabase.com:5432/postgres
    username: postgres.igezbdjyjnkurxtawmvw
    password: CunaMas_12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    time-zone: America/Lima

security:
  jwt:
    secret: "cunamas-secret-asdasdasdas-2026-jwt-token-jwt-security"
    expiration: 1800

server:
  port: 8080
  servlet:
    context-path: /api

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    config-url: /api/v3/api-docs/swagger-config
    url: /api/v3/api-docs
  api-docs:
    path: /v3/api-docs